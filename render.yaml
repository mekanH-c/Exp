services:
  - type: web
    name: aquag-command-center
    env: python
    region: oregon
    plan: free
    buildCommand: " pip install -r requirements.txt\
 startCommand: \uvicorn backend.main:app --host 0.0.0.0 --port \
 envVars:
 - key: PYTHON_VERSION
 value: \3.11.9\
